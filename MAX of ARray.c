#include<stdio.h>

int main()
{
    ,size,int arr[],int max;

    printf("Enter Array Size: ");
    scanf("%d",size);

    arr[size];

    printf("Enter array Elements: ")
    for(i=0;i<size;i++)
    {
        scanf("%d",arr[i]);
    }

    for(i=0;i<size;i++)
    {
        if(arr[i]>max)
        {
           arr[i]=max;
           printf("%d",max);
        }
        return 0;
    }
}
